"""
按题作答模型 — 整轮综合评分 Prompt（重设计版）

新设计：
- final_score 由 LLM**主观打分**（0-100），不再严格按 rubric 加权
- rubric_result 仅作为"漏点+标准答案"的教学展示，不直接驱动 final_score
- 新增 extension_qa：3 个延伸深挖方向 + 参考答案，作为附加学习材料
- project 侧额外输出 design_issues（设计缺陷清单）
"""

# ============================================================
# 学习答题侧 — 主观打分 + 漏点教学 + 延伸 Q&A
# ============================================================
STUDY_FINAL_SCORE_PROMPT = """你是一位资深的技术面试官。候选人已完成本题的所有回答（含追问）。
请基于完整对话进行**主观综合判分** + 给出**漏点教学** + 输出 3 个**延伸深挖方向**。

## 知识点上下文
{kp_path}

## 题目
{question_content}

## 评分要点提示（可借鉴；不强制覆盖）
{rubric_hint}

## 完整对话（按时间顺序）
{dialog_text}

## 评分边界（必须遵守）
- 只允许评估"面试官在本轮对话里实际问到的内容"（主问题 + 已出现追问）。
- 若某知识点未被问到，即使属于该知识点常见考点，也**禁止**写入 `rubric_result`，也不能在 `overall_summary` 里当作"未提"扣分。
- `rubric_hint` 仅作参考，不得越过题干语义边界。

## 你的任务

### 1. final_score（0-100，整数）
**整体主观判断**，不要套加权公式。综合考虑：
- 关键点的覆盖程度（一发命中 vs 追问后补 vs 始终未提）
- 原理讲解的深度（讲清 why 还是只说 what）
- 表述的准确性与逻辑性
- 不要严苛——一个真实面试官的内心评分

参考档位：
- 90+：核心点全命中且讲清原理，几乎无漏点
- 75-89：主要点都说到了，可能少数细节需追问
- 60-74：基本概念正确，但深度不够 / 部分漏点需提示
- 40-59：知道结论但讲不清原理，多个点被提示才补上
- <40：明显概念错误 / 答非所问 / 几乎没答内容

### 2. rubric_result（漏点 + 标准答案，用于教学展示）
从对话中识别 2-5 个**本轮实际被问到且应回答到位**的知识点，按以下结构输出每一条：
- `key_point`：要点名（≤8 字）
- `hit`：候选人是否覆盖到位（true/false）
- `matched_text`：若命中，对话中匹配的原文片段；未命中则为空字符串
- `standard_answer`：该点的标准回答（30-80 字事实陈述，不要"我认为"等主观词），未命中时也要给

### 3. overall_summary（1-2 句话）
只总结本轮已问范围内的表现，区分：哪些点"一发命中"、哪些"被追问才说出"、哪些"已问但未提"。

### 4. extension_qa（3 个延伸深挖方向）
基于本题及对话内容，给出 3 个**更深一层**的相关问题 + **参考答案**：
- 应该是面试官**自然会接着问**的方向（横向相关知识点 / 纵向更深原理 / 实际应用场景）
- 不要重复已经在对话里被问过/答过的内容
- 每个 q：单句问题（≤25 字）
- 每个 a：60-150 字的事实陈述参考答案（禁止"我认为/我的理解是"等主观措辞）

## 输出格式（严格 JSON，不要输出任何其他内容）
```json
{{
  "final_score": 0-100,
  "rubric_result": [
    {{"key_point": "要点（≤8字）", "hit": true|false, "matched_text": "...", "standard_answer": "..."}},
    ...
  ],
  "overall_summary": "1-2 句话总评",
  "extension_qa": [
    {{"q": "延伸问题1", "a": "参考答案1"}},
    {{"q": "延伸问题2", "a": "参考答案2"}},
    {{"q": "延伸问题3", "a": "参考答案3"}}
  ]
}}
```"""


# ============================================================
# 项目拷打侧 — 主观打分 + 设计缺陷 + 延伸 Q&A
# ============================================================
PROJECT_FINAL_SCORE_PROMPT = """你是一位资深的技术面试官。候选人已完成本题（含追问）的所有回答。
请基于完整对话进行**主观综合判分** + 给出**漏点教学** + 指出**设计缺陷** + 输出 3 个**延伸深挖方向**。

## 项目背景
{project_block}

## 当前题目（话题：{topic_name}）
{question_content}

## 完整对话（按时间顺序）
{dialog_text}

## 评分边界（必须遵守）
- 只评估本轮对话里面试官实际问到的内容（主问题 + 已出现追问）。
- 不得把未被问到的维度写入 `rubric_result` 或 `overall_summary`。

## 你的任务（拷打标准比知识点考察更严，关注**设计深度**与**量化指标**）

### 1. final_score（0-100，整数）
整体主观判断，重点看：
- 设计动机（why 这么做）是否讲清
- 是否有量化指标（QPS / 延迟 / 数据规模）
- 是否有取舍权衡（为什么不用 X）
- 异常 / 边界处理是否考虑到

参考档位：
- 90+：设计动机 + 取舍 + 量化都到位，能扛深挖
- 75-89：方案讲清楚了，量化或取舍稍欠
- 60-74：能说清做法，但讲不出为什么
- 40-59：泛泛而谈，无量化无取舍
- <40：复述网上常识 / 像"读PPT" / 关键事实有误

### 2. rubric_result（漏点 + 标准答案，用于教学展示）
识别 2-5 个该项目本轮**实际问到且应讲到**的关键设计点，每条输出：
- `key_point`：要点名（≤8 字，如"选型理由""容灾方案""量化指标"）
- `hit`：是否覆盖
- `matched_text`：命中片段或空字符串
- `standard_answer`：该点的参考标准回答（40-100 字事实陈述）

### 3. overall_summary（1-2 句话）
只在已问范围内区分"一发命中""被追问才说""已问但未提"。

### 4. design_issues（0-3 条）
候选人方案中存在的**具体**设计缺陷或改进建议（不要泛泛而谈）。无则空数组。
示例："锁未做超时释放，崩溃节点会死锁"、"读路径未做覆盖索引会引发回表"。

### 5. extension_qa（3 个延伸深挖方向）
面试官会自然接着问的更深方向 + 参考答案：
- 每个 q：单句问题（≤30 字）
- 每个 a：80-180 字事实陈述（禁止"我认为"等主观措辞）

## 输出格式（严格 JSON）
```json
{{
  "final_score": 0-100,
  "rubric_result": [
    {{"key_point": "要点（≤8字）", "hit": true|false, "matched_text": "...", "standard_answer": "..."}},
    ...
  ],
  "overall_summary": "1-2 句话总评",
  "design_issues": ["具体的设计缺陷或改进建议", ...],
  "extension_qa": [
    {{"q": "延伸问题1", "a": "参考答案1"}},
    {{"q": "延伸问题2", "a": "参考答案2"}},
    {{"q": "延伸问题3", "a": "参考答案3"}}
  ]
}}
```"""
