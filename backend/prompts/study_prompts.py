"""
学习对话相关 Prompt 模板
所有 prompt 用中文，面向中文开发者面试场景
"""

# ---- 动态出题 Prompt ----
GENERATE_QUESTION_PROMPT = """你是一位资深的技术面试官，正在面试一位候选人。请针对知识点"{knowledge_point}"用面试官的口吻出一道面试题。

## 已考过的题目与得分
{history}

## 出题要求
1. 用面试官的口吻提问，简洁直接，像真实面试中的提问方式。例如："说一下Redis分布式锁怎么实现？"、"HashMap的底层原理讲一下"、"MySQL索引什么时候会失效？"
2. 不要加"请详细描述""请给出方案"等冗长修饰，保持口语化
3. 如果有历史记录，根据遗漏点出新角度的题，避免重复
4. 如果没有历史记录，从最基础、最高频的面试题开始
5. 难度递进：之前得分高则出更深入的题，得分低则换个基础角度
6. 同时生成 4-5 个 Rubric 评分关键点，每个关键点有对应分值（通常 20-25 分）
7. **所有关键点的分值之和必须恰好等于 100 分，不能多也不能少**
8. **必须生成至少 4 个关键点**，不能只生成 2-3 个就停

请严格按以下 JSON 格式输出，不要输出其他内容：
```json
{{
  "question": "面试题内容",
  "rubric": [
    {{"key_point": "关键点描述", "score": 分值}},
    ...
  ]
}}
```"""

# ---- Rubric 打分 Prompt（含追问决策）----
RUBRIC_SCORING_PROMPT = """你是一位资深的技术面试官，正在对候选人的回答进行评分。

## 题目
{question}

## 评分标准（Rubric）
以下是评分关键点，每个关键点有对应分值。请逐个判断候选人是否提到了该关键点。
{rubric_items}

## 候选人回答
{user_answer}

## 评分要求
1. 用户输入可能含错别字（来自语音输入），请按语义理解，不要因为错别字扣分
2. 只要候选人表达了关键点的核心意思，即使措辞不同也算命中
3. 对每个关键点给出：是否命中(hit)、候选人原文中匹配的部分(matched_text，未命中则为空字符串)
4. 如果候选人提到了某关键点但表述有误，仍算命中(hit=true)
5. **total 必须等于所有命中(hit=true)的关键点的 score 之和，满分 100**（如果 rubric 总分不足 100，按比例折算到 100 分制）
6. **追问决策**：如果候选人有重要遗漏（得分<80），用面试官口吻针对最关键的遗漏点追问（简洁直接）；如果回答较完整（得分>=80），设 follow_up 为 null
7. **生成推荐回答**：分点列出这道题的标准回答要点，每个要点一句话，共 3-5 个要点

请严格按以下 JSON 格式输出，不要输出其他内容：
```json
{{
  "items": [
    {{
      "key_point": "关键点描述（8字以内）",
      "score": 分值,
      "hit": true/false,
      "matched_text": "候选人回答中对应的原文片段"
    }},
    ...
  ],
  "total": 总得分,
  "feedback": "总体反馈，1句话，20字以内",
  "recommended_answer": ["要点1", "要点2", "要点3"],
  "follow_up": "追问问题（面试官口吻）或 null",
  "follow_up_rubric": [
    {{"key_point": "追问的关键点", "score": 分值}},
    ...
  ]
}}
```"""
