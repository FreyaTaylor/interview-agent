"""
知识树初始化 Prompt 模板
- 骨架生成（一级+二级分类）
- 叶子展开（三级知识点+权重）
"""

# Step 1: 生成一级+二级骨架
TREE_SKELETON_PROMPT = """你是一位资深 Java 后端面试辅导专家。请根据用户画像，生成一棵面试知识树的**一级分类 + 二级分类**骨架。

## 用户画像
{profile_text}

## 要求
1. 一级分类 **8-12 个**，覆盖 Java 后端面试核心领域，根据用户画像调整侧重
2. 每个一级分类下的二级分类数量**根据该领域的知识丰富度自行决定**（MySQL/Redis 等内容多的可以 4-6 个，计算机网络等偏少的 2-3 个即可）
3. **二级分类名称简短**（≤8字），不要在名称中列举具体技术（❌ "JUC核心工具（AQS、ReentrantLock）" → ✅ "并发工具类"）
4. **不要太偏门**：容器化/云原生/服务网格等除非用户画像明确提到，否则不生成
5. **不生成「项目经验」「项目实战」类分类**，项目相关内容从面试复盘模块获取
6. 不需要生成三级叶子节点（下一步单独展开）
7. 排序：高频考点靠前
8. **设计模式**如果作为一级分类，二级只保留高频的（创建型、结构型、行为型各取 1-2 个高频），不要全列

### 正确示例 ✅
一级"Java并发" → 二级: 线程基础 | 锁机制 | 并发工具类 | 线程池 | 并发容器
一级"MySQL" → 二级: 索引原理 | 事务与锁 | SQL调优 | 日志机制 | 分库分表 | 主从与高可用（6 个，因为 MySQL 内容多）
一级"计算机网络" → 二级: TCP/IP | HTTP（2 个，因为内容相对少）

### 错误示例 ❌
一级"Java并发编程" → 二级: "JUC核心工具（AQS、ReentrantLock、CountDownLatch、CyclicBarrier、Semaphore）"（名称太长，把叶子塞进二级了）
每个一级都生成 4 个二级（太均匀，MySQL 和计网不应该一样多）

## 输出格式
```json
{{
  "categories": [
    {{
      "name": "一级分类名称",
      "sort_order": 1,
      "children": [
        {{"name": "二级分类名称", "sort_order": 1}},
        {{"name": "二级分类名称", "sort_order": 2}}
      ]
    }}
  ]
}}
```

只返回 JSON，不要其他内容。"""


# Step 2: 展开分类为面试考点树（一次返回，LLM自行决定层级深度）
TREE_EXPAND_PROMPT = """你是一位资深 Java 后端面试官。请列出「{node_path}」这个方向下，**面试中会考到的具体考点**。

## 用户画像
{profile_text}

## 已有的考点（全局去重）
{existing_leaves}

## ⚠️ 重要约束
**只列「{node_path}」方向下的面试考点！不要跨方向！**

## 核心规则

**每个叶子节点必须是一个"面试考点"**——面试官可以围绕它出 1-3 道题的最小单元。

### 判断标准：面试官会不会单独围绕它提问？
- ✅ "三次握手" — 面试官会问"讲一下三次握手过程"
- ✅ "MVCC" — 面试官会问"MVCC怎么实现的"  
- ✅ "CAS原理" — 面试官会问"CAS是什么，有什么问题"
- ❌ "面向连接" — 面试官不会单独问"什么是面向连接"，这是TCP的特性描述
- ❌ "序号与确认号" — 面试官不会单独问这个，这是报文字段
- ❌ "源端口与目的端口" — 这是知识描述，不是考点

### 层级规则
- 如果考点较多，可以用子分类归组（有 children）
- 如果考点较少，直接列叶子（无 children）
- **最深不超过 2 层**（避免过度细分）
- 子分类本身也应该是面试会考的方向（如"锁升级"是子分类，面试官会问"synchronized锁升级过程"）

## 要求
1. 名称简短（≤10字），是面试官会直接提到的词
2. 每个节点标注面试权重 1-5
3. 数量根据实际考频决定，不凑数
4. 不要和已有考点重复
5. 只放大厂 3 年经验 Java 后端高频考点

## 输出格式（嵌套结构）
```json
{{
  "children": [
    {{
      "name": "子分类名称",
      "interview_weight": 5,
      "children": [
        {{"name": "面试考点", "interview_weight": 5}},
        {{"name": "面试考点", "interview_weight": 4}}
      ]
    }},
    {{
      "name": "直接考点", 
      "interview_weight": 4
    }}
  ]
}}
```

有 children = 子分类，没有 children = 叶子考点。只返回 JSON。"""
