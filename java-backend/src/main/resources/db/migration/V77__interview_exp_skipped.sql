-- =============================================================================
-- V77: 「看看面经」问题「不用看」标记 skipped
--
-- 背景：有些面经问题一看就没意义、不必学；用户想一键标记「不用看」，把噪声问题折叠掉。
-- 现象：面经里混着大量不值得学的问题，全看一遍浪费精力。
-- 根因：缺一个二值标记表达「这题没必要学」。
-- 修复：内容侧表新增 skipped（与问题 1:1，随 tree_node 级联删）；🚫 图标点一下二值反转。
--       域级「不用看」由前端派生（该域下所有问题都 skipped → 域也置灰），不单独存列。
-- =============================================================================

ALTER TABLE interview_exp_question_detail
    ADD COLUMN skipped BOOLEAN NOT NULL DEFAULT false;  -- 「不用看」标记（🚫 二值反转）
