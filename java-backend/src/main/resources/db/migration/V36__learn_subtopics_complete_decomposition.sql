-- V36: 子话题从"多角度"改为"完整拆解（MECE 子知识点）"
-- 背景：V31/V35 让模型按"定义辨析/底层原理/使用场景/性能对比"等【通用角度】套模板，
--   结果是"从 N 个角度看同一个点"，空泛、易重复，也不符合用户预期。
-- 用户诉求：要的是把知识点拆成它【真实的组成部分/子知识点】，这些子话题
--   ①合起来 = 面试会考的全部内容（完整、不遗漏）②互不重复（不交叉、不换皮）。
--   例（线程池）：7 个核心参数 / 任务提交与线程分配流程 / 拒绝策略 / 生命周期 …
-- 修复：把 prompt 核心从"面试切入维度"改为"按知识点自身知识结构做完整拆解"，
--   保留 V35 的标题唯一 + 不嵌面试问行等硬规则。

INSERT INTO prompt_template (key, content, description) VALUES
('learn/subtopics-gen', $PROMPT$你是一位资深技术面试辅导专家。请把下面这个知识点，拆成一套"面试导向"的**完整子话题清单**，**必须只返回 JSON 数组**。

## 知识点
{knowledge_point}

## 所属分类路径
{category_path}

## ❗❗ 领域约束
**必须严格按「所属分类路径」确定知识点的技术领域！**
- 路径以 mysql 开头 → 讲 MySQL 相关内容，不要讲 Java
- 路径以 redis 开头 → 讲 Redis 相关内容
- 路径以 Java 开头 → 讲 Java 相关内容
- 即使知识点名称和其他领域有同名概念，也必须讲当前路径对应技术的版本
- 例：mysql → 连接数与线程池 → 讲 MySQL 的线程池（thread_pool 插件、连接管理），不是 Java ThreadPoolExecutor

## ❗❗❗ 核心目标：完整拆解（不是"从多个角度看同一个点"）
把这个知识点拆成它**真实的组成部分 / 子知识点**，让整套清单同时满足：
- **完整（合起来 = 全部）**：这些子话题拼起来，应覆盖面试官就该知识点会考到的**全部核心内容**，不遗漏关键模块。
- **不重复（互不交叉）**：任意两条讲的是**不同的子知识点**，不换皮、不交叉。
每一条都应是"这个知识点里一块实打实的内容"，而**不是**"看待它的一个角度"。

### 怎么拆（关键）
按知识点**自身的知识结构**拆，常见切法：核心组成 / 关键参数、主流程 / 步骤、分类 / 种类、策略 / 算法、生命周期 / 状态、底层数据结构、典型问题 / 坑。
**不要**套"定义辨析 / 底层原理 / 使用场景 / 性能对比"这种放之四海的通用角度模板去凑——那样会空泛、重复、且不像真正的知识拆解。

### 范例（照这个颗粒度和拆法）
- **线程池** → ["7 个核心参数", "任务提交与线程分配流程", "阻塞队列的作用与选型", "拒绝策略", "线程池生命周期与状态流转", "常见线程池类型（Executors）", "线程复用原理"]
- **HashMap** → ["底层数据结构（数组+链表+红黑树）", "put 流程与哈希计算", "扩容机制（resize）", "链表转红黑树阈值", "并发下的问题", "常见 hash 冲突处理"]
- **synchronized** → ["用法与作用范围", "对象头与 Monitor", "锁升级（偏向/轻量/重量）", "可重入原理", "与 ReentrantLock 的区别"]

## 去重 + 完整 自检（输出前必做）
- 逐条看：有没有两条其实是同一块内容换了说法？有 → 合并。
- 通盘看：把整套清单合起来，面试官常问的点是否基本都覆盖了？缺关键模块 → 补上。
- **完全相同或高度相似的 title 绝对不允许出现两次。**

## 输出 schema（严格）
```json
[
  {
    "title": "一个具体的子知识点（≤25 字，全列表唯一）",
    "body_md": "Markdown 正文，2-4 段简洁专业，关键词 **加粗**；可含 ≤10 行代码块或对比表",
    "importance": 4
  }
]
```

## 字段要求
- `title`：必填，非空，不含 `#` 标题符，**全列表唯一**，是一个**具体子知识点**（不是"角度"）
- `body_md`：必填，纯 Markdown，**不要**再嵌套 `####` 子标题（每个子话题就是一个独立单元）
- `importance`：整数 1-5，面试出现频率（5=必考、4=高频、3=常考、2=偶现、1=冷门）

## 数量与覆盖
- 条数 = **覆盖完整所需的条数**，通常 5-10 条；**不为凑数、也不遗漏关键模块**。
- 按"由浅入深 / 面试常考顺序"排列。

## 风格
- body_md 关键词 `**加粗**`、要点 ✅/❌、每段 ≤3 句、每句 ≤30 汉字
- **不要**生成"总结"/"小结"子话题
- **不要**在 body 里嵌入任何"面试常问 / 面试追问"问题行（无论什么标记，一律不放）——面试追问后续走探索对话累积

只返回 JSON 数组，不要 Markdown 围栏、不要解释文字。$PROMPT$, 'Learn 模块：知识点完整拆解（MECE 子知识点，完整不重复）')
ON CONFLICT (key) DO UPDATE SET content = EXCLUDED.content, description = EXCLUDED.description;
