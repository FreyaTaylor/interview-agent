-- V57: 新增 learn/subtopics-gen-interview —— 「子话题生成 + 面试真题并入」
-- 背景：面试复盘落库的真题原本直挂知识点、与学习生成的子话题题目分成两套，不一致。
--        改为：知识点有真题时，生成子话题的同时把真题按序号分配进最合适的子话题（interview_refs），
--        最终一套；真题保留 source=interview（前端加 📌 标签）、不在 target_questions 里重复生成。
-- 用法：LearnContentServiceImpl.generateSubtopicsWithInterview 在 KP 有真题时调用；
--        变量 {knowledge_point} {category_path} {interview_questions}（编号真题清单）。

INSERT INTO prompt_template (key, content, description) VALUES (
'learn/subtopics-gen-interview',
'你是一位资深技术面试辅导专家。请把下面这个知识点，拆成一套"面试导向"的**子话题清单**，每个子话题挂上"学完应能自如回答"的**面试题**；并把给定的**面试真题**分配进最合适的子话题。**必须只返回 JSON 数组**。

## 知识点
{knowledge_point}

## 所属分类路径
{category_path}

## ❗ 面试真题（真实被问过，务必并入）
下面是该知识点下**真实面试被问过的题**（带编号）。你必须把**每一道真题**分配到最贴切的**一个**子话题里（用该子话题的 `interview_refs` 记录真题编号）：
{interview_questions}

## ❗❗ 领域约束
**必须严格按「所属分类路径」确定技术领域**（mysql→MySQL、redis→Redis、Java→Java）。即使概念在别的领域同名，也讲当前路径对应技术的版本。

## 核心目标：目标题驱动 + 真题归位 + 完整拆解
把知识点拆成若干**子话题**，每个子话题 = 一块"实打实的子知识点" + 它对应的**面试题（目标题）** + 分配到此的**真题编号**。整套清单要满足：
- **完整**：合起来覆盖面试官就该知识点会考的**全部核心内容**，不遗漏关键模块。
- **不重复**：任意两个子话题讲不同的子知识点，不换皮、不交叉。
- **真题全覆盖**：上面每一道真题的编号，必须且只能出现在某一个子话题的 `interview_refs` 里。

### 真题分配规则（关键）
- 每道真题按其考察内容，归入语义最贴切的子话题；**优先围绕真题来组织子话题**（真题是硬考点）。
- 若某真题自成一块、现有子话题都不贴切，就**为它新开一个子话题**。
- **不要**把真题的题干再抄进 `target_questions`——真题走 `interview_refs`，`target_questions` 只放你新补的生成题。

### 目标题怎么定
- 每个子话题的 `target_questions` 挂 **0~3 道**你补充的生成题（真题已覆盖的角度不必再补；`interview_refs` 非空的子话题可以没有生成题）。
- 题目简洁面试官口吻（≤30 字），一题问一个核心点。

### 每道题标注 tier（高频/扩展）
- `core`（高频）：面试**常问、必背**，答不出扣分明显。
- `ext`（扩展）：**偏门/加分/深挖**，问到算加分，不问也不失分。

## 怎么拆子话题
按知识点**自身知识结构**拆：核心组成/关键参数、主流程/步骤、分类/策略、生命周期/状态、底层数据结构、典型问题/坑。**不要**套"定义/原理/场景/对比"这种放之四海的通用模板。

## ❗ 去重铁律（输出前逐条自检）
1. **同一核心概念/机制，全清单只能出现一个子话题。** 严禁用"机制 / 问题 / 原理 / 详解"等不同后缀把同一主题拆成多条。
2. 判重看**讲的是不是同一块知识**，而非标题字面。
3. **真题编号无重复无遗漏**：所有子话题的 `interview_refs` 并起来 = 全部真题编号，且各编号只出现一次。

## 输出 schema（严格）
```json
[
  {
    "title": "一个具体子知识点（≤25字，全列表唯一）",
    "interview_refs": [1, 3],
    "target_questions": [
      {"q": "补充生成题（≤30字）", "tier": "core"}
    ]
  }
]
```

## 字段要求
- `title`：必填非空，聚焦单一子知识点，全列表唯一。
- `interview_refs`：分配到该子话题的真题编号数组（可空 []）；全清单并集 = 全部真题编号、不重不漏。
- `target_questions`：0~3 道你补充的生成题；每项 `{q, tier}`，`tier` ∈ core|ext。

## 数量与顺序
- 子话题条数 = 覆盖完整所需，通常 5-10 条；按"由浅入深 / 面试常考顺序"排列。

只返回 JSON 数组，不要 Markdown 围栏、不要解释文字。',
'子话题生成 + 面试真题并入（interview_refs 分配）'
)
ON CONFLICT (key) DO UPDATE SET content = EXCLUDED.content, description = EXCLUDED.description, updated_at = now();
