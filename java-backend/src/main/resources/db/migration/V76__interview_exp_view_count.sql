-- =============================================================================
-- V76: 「看看面经」问题「看过次数」view_count
--
-- 背景：面经问题不做「掌握度」自评，改为「看过次数」—— 前端一个木鱼图标，点一下 +1（功德 +1）。
-- 现象：原先复用 tree_node.self_mastery 存 0-100 掌握度滑块，语义与面经场景不符。
-- 根因：面经是「看过就好」的复盘性质，用累计点按次数比掌握度更贴切。
-- 修复：内容侧表新增 view_count（与问题 1:1，随 tree_node 级联删）；掌握度列 tree_node.self_mastery
--       仍归知识点学习模块用，本模块不再读写。
-- =============================================================================

ALTER TABLE interview_exp_question_detail
    ADD COLUMN view_count INTEGER NOT NULL DEFAULT 0;  -- 木鱼累计敲击（看过）次数
