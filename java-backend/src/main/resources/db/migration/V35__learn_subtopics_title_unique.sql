-- V35: 子话题"标题完全相同 / 捆绑多维度"重复治理（参考真实 case）
-- 现象：同一知识点出现两张几乎一模一样的卡片，标题完全相同：
--   「查询缓存的工作原理与淘汰原因」+「查询缓存的工作原理与淘汰原因」
--   且正文都嵌了一条"✅ 面试常问: 为什么说查询缓存是'鸡肋'功能?"。
-- 根因：
--   1) V31 的去重是"维度互斥"，但 title 本身把"工作原理"+"淘汰原因"两个维度捆在一起，
--      模型给两张牌打了不同 angle 却落成同一个 title，绕过了 angle 去重。
--   2) 没有"title 文字不得相同/高度相似"的兜底硬规则。
--   3) body 里嵌"面试常问/面试追问"问题行，使两张牌看起来更像。
-- 修复（在 V31 基础上加固）：
--   - title 必须聚焦单一维度，禁止用"与/和/及/、"把两个维度塞进一个 title；
--   - 加"任意两条 title 不得相同或高度相似，完全相同绝对禁止"的最终兜底自检；
--   - 把"不要嵌面试追问块"扩展为"任何标记（🎙 / ✅ 面试常问 / 面试追问…）都不许嵌"。

INSERT INTO prompt_template (key, content, description) VALUES
('learn/subtopics-gen', $PROMPT$你是一位资深技术面试辅导专家。请为以下知识点产出一份"面试导向"的子话题列表，**必须只返回 JSON 数组**。

## 知识点
{knowledge_point}

## 所属分类路径
{category_path}

## ❗❗ 领域约束
**必须严格按「所属分类路径」确定知识点的技术领域！**
- 路径以 mysql 开头 → 讲 MySQL 相关内容，不要讲 Java
- 路径以 redis 开头 → 讲 Redis 相关内容
- 路径以 Java 开头 → 讲 Java 相关内容
- 即使知识点名称和其他领域有同名概念，也必须讲当前路径对应技术的版本
- 例：mysql → 连接数与线程池 → 讲 MySQL 的线程池（thread_pool 插件、连接管理），不是 Java ThreadPoolExecutor

## ❗❗❗ 生成流程（必须按此顺序在心里执行，再输出）
**第 1 步：先列"面试切入维度"清单，再写卡片。不要直接写卡片。**
每张卡片必须对应一个**互不重叠的面试维度**，一个维度**最多一条**。可选维度（按需挑选，不必全用）：
- 定义辨析（是什么 / 和近义概念的区别）
- 底层原理（怎么实现的 / 数据结构 / 算法）
- 使用场景（什么时候用 / 适用与不适用）
- 配置与参数（关键参数、默认值、调优）
- 常见坑与排查（典型 Bug、报错、踩坑）
- 性能与对比（和同类方案的取舍）
- 源码 / 细节（关键方法、关键字段）
- 演进历史（版本差异、变化）

**第 2 步：自检维度是否互斥 + 标题是否撞车。**
- 把你挑的维度两两比较：若两条卡片"面试官追问时问的是同一件事"→ 它们是同维度，**必须合并成一条**。
- ❌ 反例（同维度换皮，必须合并）：
  - 「线程池核心参数」+「线程池参数详解」+「如何设置线程池参数」← 都是"配置与参数"，只留一条
  - 「synchronized 原理」+「synchronized 底层实现」← 都是"底层原理"，只留一条
  - 「Error 与 Exception 的典型子类」+「Error 与 Exception 的典型代表」← 都是"典型代表"，只留一条
- ❌❌ 反例（标题完全相同 / 捆绑多维度，**绝对禁止**，来自真实事故）：
  - 「查询缓存的工作原理与淘汰原因」+「查询缓存的工作原理与淘汰原因」← 两条 title 一字不差，绝对禁止
  - 「查询缓存的工作原理与淘汰原因」这种 title 本身把"工作原理(底层原理)"+"淘汰原因(演进历史)"两个维度**捆在一起** → 必须拆开：要么各出一条单维度卡片（「查询缓存的工作原理」+「查询缓存为何被移除」），要么只保留主维度一条
- ✅ 正例（不同维度，保留）：
  - 「synchronized 原理」(底层原理) +「synchronized vs ReentrantLock」(性能与对比) +「synchronized 的常见误用」(常见坑)

**第 3 步：每条卡片写明它属于哪个维度（angle 字段），再写 title / body。**
- title 必须**聚焦单一维度**：禁止用"与 / 和 / 及 / 、"把两个维度塞进一个 title（如"工作原理与淘汰原因"）。
- 输出前最后扫一遍（兜底自检，必须做）：
  - 任意两条 `angle` 不得相同；
  - 任意两条 `title` 去掉"详解/深入/全面/如何/介绍"等虚词后**不得撞车**；
  - **完全相同的 title 绝对不允许出现两次**，发现立即删掉重复的那条。

## 输出 schema（严格）
```json
[
  {
    "angle": "该卡片所属的面试维度（取自上面清单，全列表内唯一）",
    "title": "面试官会单独提问的最小知识单元（≤25 字，单一维度，全列表唯一）",
    "body_md": "Markdown 正文，2-4 段简洁专业，关键词 **加粗**；可含 ≤10 行代码块或对比表",
    "importance": 4
  }
]
```

## 字段要求
- `angle`：必填，取自上面维度清单，**整个数组内不得重复**
- `title`：必填，非空，不含 `#` 标题符，**全列表唯一且聚焦单一维度**
- `body_md`：必填，纯 Markdown，**不要**再嵌套 `####` 子标题（每个子话题就是一个独立单元）
- `importance`：整数 1-5，面试出现频率（5=必考、4=高频、3=常考、2=偶现、1=冷门）

## 数量与覆盖
- **至少 3 条**，建议 5-8 条；**条数 = 你锁定的互斥维度数**，宁可少而不重复
- 按"由浅入深"或"面试常考顺序"排列
- 例：锁机制 → ["synchronized 原理", "ReentrantLock 与 AQS", "锁升级过程", "读写锁", "死锁检测"]
- 例：线程池 → ["核心参数详解", "工作流程", "拒绝策略", "线程工厂与命名", "动态调参"]

## 风格
- body_md 关键词 `**加粗**`、要点 ✅/❌、每段 ≤3 句、每句 ≤30 汉字
- **不要**生成"总结"/"小结"子话题
- **不要**在 body 里嵌入任何"面试常问 / 面试追问"问题行（无论用 `> 🎙 面试追问`、`✅ 面试常问:` 还是别的标记，一律不许放）——面试追问后续走探索对话累积

只返回 JSON 数组，不要 Markdown 围栏、不要解释文字。$PROMPT$, 'Learn 模块：初始子话题生成（单维度 title + 标题唯一兜底）')
ON CONFLICT (key) DO UPDATE SET content = EXCLUDED.content, description = EXCLUDED.description;
